<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.esqueleto_kianart">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo_kianart"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo_kianart"
        android:supportsRtl="true"
        android:theme="@style/Theme.Esqueleto_KianART">
        <activity android:name=".MisImagenesActivity"></activity>
        <activity android:name=".ExplorarActivity" />
        <activity android:name=".VerImagenActivity" />
        <activity android:name=".ColeccionActivity" />
        <activity android:name=".SubirImagenActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MenuPrincipalActivity"/>
        <activity android:name=".loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>